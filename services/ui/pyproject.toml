[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "raglab-ui"
version = "0.1.0"
description = "RAGLab Control Panel — Jinja2 + Alpine.js"
requires-python = ">=3.12"
dependencies = [
    "raglab-common",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
    "python-multipart>=0.0.9",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ui"]
